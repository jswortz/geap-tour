"""GEAP Workshop — src package.

Project and location are always configured explicitly (see ``src/config.py`` and
the ``Client(project=..., location=...)`` / ``vertexai.init(project=..., location=...)``
call sites), so no runtime monkey-patching of the Google Cloud SDKs is required.
"""
