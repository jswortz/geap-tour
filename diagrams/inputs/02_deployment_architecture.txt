Create a deployment architecture diagram for Google Cloud Platform.

Layout (top to bottom):
- Top section "Users": A user icon with arrow pointing down

- Ingress layer: A horizontal bar labeled "Ingress Gateway (Client-to-Agent)" with a gateway icon
  - Sub-label: "AuthN, Rate Limiting, Request Validation"
  - Arrow from Users down through Ingress Gateway

- Middle section "Vertex AI Agent Runtime": Three boxes for deployed agents (Travel Agent, Expense Agent, Coordinator) inside a blue bordered region labeled "Agent Engine". Each box shows "AdkApp + OTel Tracing"
  - "SPIFFE Identity" badge on Agent Runtime region

- Egress layer: A horizontal bar labeled "Egress Gateway (Agent-to-Anywhere)" with a gateway/shield icon
  - Sub-label: "IAM Allow Policies, SGP Enforcement"
  - Arrows from Agent Runtime boxes down through Egress Gateway

- Bottom section "Destinations" (three columns):
  - Left column "Gemini Models": Vertex AI icon, label "gemini-2.5-flash / pro"
  - Center column "Cloud Run MCP Servers": Three service boxes (Search MCP :8001, Booking MCP :8002, Expense MCP :8003) inside a green bordered region
  - Right column "External APIs": Globe icon, label "Governed by SGP"

- Arrows from Egress Gateway down to each destination column

- Side annotations:
  - "OTel → Cloud Trace" arrow going right from Agent Runtime to a Cloud Trace icon

Style: Google Cloud official architecture diagram style. Use GCP product icons where possible. White background, professional colors. Orange for ingress gateway bar, red-orange for egress gateway bar. Title: "Deployment Architecture"
